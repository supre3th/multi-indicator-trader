---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: [01-01]
files_modified: []
autonomous: true
requirements: []
---

<objective>
Create FastAPI backend with Binance API integration and klines endpoint.
</objective>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FastAPI app structure</name>
  <files>backend/app/main.py, backend/app/api/klines.py</files>
  <action>
Create FastAPI application:
1. Create backend/app/__init__.py
2. Create backend/app/main.py with FastAPI app, CORS config for localhost:3000
3. Create backend/app/api/__init__.py
4. Create backend/app/api/klines.py with endpoint: GET /api/klines?symbol=BTCUSDT&interval=1h&limit=100
5. Create backend/app/services/__init__.py
6. Create backend/app/services/binance.py with CCXT integration
7. Create .env.example with BINANCE_API_KEY, BINANCE_SECRET_KEY (optional for public data)
  </action>
  <verify>
    <automated>cd backend && python -c "from app.main import app; print('FastAPI app loads')"</automated>
  </verify>
  <done>FastAPI app runs and responds to requests</done>
</task>

<task type="auto">
  <name>Task 2: Implement Binance klines endpoint</name>
  <files>backend/app/services/binance.py, backend/app/api/klines.py</files>
  <action>
Implement Binance data fetching:
1. Use CCXT to fetch OHLCV data (public endpoint, no API key needed for historical)
2. Convert to format: {time, open, high, low, close, volume}
3. Support parameters: symbol (default BTCUSDT), interval (default 1h), limit (default 100)
4. Support intervals: 1m, 5m, 15m, 1h, 4h, 1d
5. Add error handling for invalid symbols/intervals
  </action>
  <verify>
    <automated>curl -s "http://localhost:8000/api/klines?symbol=BTCUSDT&interval=1h&limit=10" | head -c 200</verify>
  </verify>
  <done>Endpoint returns OHLCV data from Binance</done>
</task>

</tasks>

<verification>
- [ ] GET /api/klines returns candlestick data
- [ ] Supports symbol, interval, limit parameters
- [ ] CORS configured for frontend
</verification>

<success_criteria>
Backend serves Binance klines data via REST API.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
