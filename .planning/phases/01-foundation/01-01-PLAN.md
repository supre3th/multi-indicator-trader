---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []
---

<objective>
Set up Next.js frontend and FastAPI backend project structure with dependencies.
</objective>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize frontend project</name>
  <files>frontend/package.json, frontend/tsconfig.json, frontend/next.config.js</files>
  <action>
Create Next.js 15 frontend with TypeScript and Tailwind CSS:
1. Run: npx create-next-app@latest frontend --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"
2. Install dependencies: npm install lightweight-charts zustand @tanstack/react-query lucide-react clsx tailwind-merge papaparse date-fns duckdb-wasm
3. Initialize shadcn: npx shadcn-ui@latest init (use default options, dark theme)
4. Add components: npx shadcn-ui@latest add button dropdown-card select toggle
  </action>
  <verify>
    <automated>ls frontend/package.json && cat frontend/package.json | grep -q "next" && cat frontend/package.json | grep -q "lightweight-charts"</automated>
  </verify>
  <done>Frontend project created with all dependencies installed</done>
</task>

<task type="auto">
  <name>Task 2: Initialize backend project</name>
  <files>backend/app/main.py, backend/requirements.txt</files>
  <action>
Create FastAPI backend project:
1. Create directory: mkdir backend && cd backend
2. Create virtual env: python -m venv venv && activate (venv\Scripts\activate on Windows)
3. Install: pip install fastapi uvicorn[standard] ccxt pandas numpy python-dotenv pydantic-settings aiohttp
4. Create app/main.py with FastAPI app structure
5. Create requirements.txt
  </action>
  <verify>
    <automated>ls backend/requirements.txt && cat backend/requirements.txt | grep -q "fastapi"</automated>
  </verify>
  <done>Backend project created with Python dependencies</done>
</task>

</tasks>

<verification>
- [ ] frontend/ directory exists with Next.js project
- [ ] backend/ directory exists with FastAPI project
- [ ] Both have package.json/requirements.txt
</verification>

<success_criteria>
Frontend and backend projects initialized with all required dependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
