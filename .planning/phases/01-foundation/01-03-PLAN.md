---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: [01-02]
files_modified: []
autonomous: true
requirements: []
---

<objective>
Build frontend chart display with symbol/timeframe selection and dark/light theme.
</objective>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create chart component with Lightweight Charts</name>
  <files>frontend/src/components/Chart.tsx, frontend/src/components/SymbolSelector.tsx, frontend/src/components/TimeframeSelector.tsx</files>
  <action>
Build chart components:
1. Create frontend/src/components/Chart.tsx - Lightweight Charts candlestick chart
2. Create frontend/src/components/SymbolSelector.tsx - dropdown with major pairs (BTCUSDT, ETHUSDT, etc.) + search for all pairs
3. Create frontend/src/components/TimeframeSelector.tsx - buttons for 1m, 5m, 15m, 1h, 4h, 1d
4. Create frontend/src/lib/api.ts - fetch wrapper for backend API
5. Create frontend/src/stores/chartStore.ts - Zustand store for symbol, timeframe, theme
  </action>
  <verify>
    <automated>npm run build --prefix frontend 2>&1 | tail -20</automated>
  </verify>
  <done>Chart component renders candlesticks</done>
</task>

<task type="auto">
  <name>Task 2: Build dashboard page with theme toggle</name>
  <files>frontend/src/app/page.tsx, frontend/src/components/ThemeToggle.tsx</files>
  <action>
Create main dashboard:
1. Update frontend/src/app/page.tsx - chart-centric layout (TradingView style)
2. Create frontend/src/components/ThemeToggle.tsx - dark/light mode switch
3. Create frontend/src/app/globals.css - add dark theme CSS variables
4. Add theme provider using next-themes
5. Wire up: symbol selector → fetch data → render chart
6. Wire up: timeframe selector → fetch data → update chart
  </action>
  <verify>
    <automated>npm run build --prefix frontend 2>&1 | grep -q "Build" && echo "Build successful"</automated>
  </verify>
  <done>Dashboard loads with chart, selectors, theme toggle</done>
</task>

</tasks>

<verification>
- [ ] Chart displays candlesticks for BTCUSDT
- [ ] Symbol change updates chart
- [ ] Timeframe change updates chart
- [ ] Dark/light theme toggle works
- [ ] Responsive chart sizing
</verification>

<success_criteria>
Working chart dashboard with symbol/timeframe selection and theme toggle.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
